{
  "name": "felix-sailing-site",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 52124",
    "build": "vite build",
    "preview": "npm run build && node preview-server.mjs",
    "test": "node --test tests/*.test.js",
    "test:browser": "npm run build && node tests/run-browser-smoke.mjs",
    "test:all": "npm run test && npm run test:browser"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.2.0",
    "playwright": "1.62.0",
    "vite": "^7.3.6"
  },
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  }
}
